// Quarantine Crystal — Modern format
// Physical/Special split active (QC custom split, see moves data).
// Abilities active (see abilities data; [TBD Ability] = placeholder).

export const Scripts: ModdedBattleScriptsData = {
	init() {
		// No engine overrides needed for modern format —
		// categories and abilities are baked into the data files.
	},
};
